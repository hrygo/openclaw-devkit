# ==============================================================================
# OpenClaw Docker Compose Development Environment
# ==============================================================================
# Quick Start:
#   ./docker-dev-setup.sh                              # 启动服务
#   docker compose -f docker-compose.dev.yml logs -f   # 查看日志
#   docker compose -f docker-compose.dev.yml down      # 停止服务
#
# Key Features:
#   - Claude Code CLI 内置
#   - 代理配置 (访问 Google/Claude API)
#   - Go/TypeScript/Python 开发环境
#   - 浏览器自动化 (Playwright)
#   - Office 文件处理 (pandoc + LaTeX)
# ==============================================================================

# ------------------------------------------------------------------------------
# YAML Anchors for DRY Configuration
# ------------------------------------------------------------------------------
x-openclaw-common: &openclaw-common
  image: ${OPENCLAW_IMAGE:-openclaw}
  environment: &common-env
    HOST_UID: ${HOST_UID:-1000}
    # ─────────────────────────────────────────────────────────────────
    # Proxy Configuration
    # ─────────────────────────────────────────────────────────────────
    # Required for accessing Google and Claude API from restricted network environments
    # Ports: 7897 (HTTP proxy), 15721 (Claude API proxy)
    # ─────────────────────────────────────────────────────────────────
    HTTP_PROXY: ${HTTP_PROXY:-}
    HTTPS_PROXY: ${HTTPS_PROXY:-}
    NO_PROXY: localhost,127.0.0.1,host.docker.internal,.local,.internal
    # ─────────────────────────────────────────────────────────────────
    # Claude API Configuration
    # ─────────────────────────────────────────────────────────────────
    ANTHROPIC_AUTH_TOKEN: ${ANTHROPIC_AUTH_TOKEN:-}
    ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
    API_TIMEOUT_MS: "3000000"
    CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: "1"
    CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS: "1"
    # ─────────────────────────────────────────────────────────────────
    # Runtime Configuration
    # ─────────────────────────────────────────────────────────────────
    PLAYWRIGHT_BROWSERS_PATH: /home/node/.cache/ms-playwright
    # NotebookLM config path (shared with host via bind mount)
    NOTEBOOKLM_STORAGE: /home/node/.notebooklm/storage_state.json
  extra_hosts:
    - "host.docker.internal:host-gateway"
  logging: &common-logging
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"
  deploy: &common-deploy
    resources:
      limits:
        memory: 4G
        cpus: '4.0'
      reservations:
        memory: 1G
        cpus: '0.5'
  restart: unless-stopped

# ------------------------------------------------------------------------------
# Named Volumes
# ------------------------------------------------------------------------------
volumes:
  # Unified home directory volume - replaces all scattered mounts
  # Contains: .global, .local, .opencode, .openclaw, .claude, .notebooklm,
  #           .agents, .cache, go/, /app (node_modules)
  openclaw-devkit-home:
    name: openclaw-devkit-home

services:
  # ============================================================================
  # OpenClaw Gateway
  # ============================================================================

  openclaw-gateway:
    <<: *openclaw-common
    user: "0:0" # Run as root to fix permissions - enabled to fix EACCES
    container_name: openclaw-gateway
    ports:
      - "127.0.0.1:${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "127.0.0.1:${OPENCLAW_BRIDGE_PORT:-18790}:18790"
      - "127.0.0.1:${OPENCLAW_BROWSER_PORT:-18791}:18791"
    env_file:
      - .env
    environment:
      <<: *common-env
      OPENCLAW_CONFIG_DIR: /home/node/.openclaw
      OPENCLAW_WORKSPACE_DIR: /home/node/.openclaw/workspace
      OPENCLAW_HOME: /home/node
      # Node.js package managers (npm/pnpm/bun)
      npm_config_prefix: /home/node/.global
      pnpm_config_global_dir: /home/node/.global/pnpm
      pnpm_config_global_bin_dir: /home/node/.global/bin
      BUN_INSTALL_PREFIX: /home/node/.global
      # Python package manager (uv) - force user-level installation
      UV_SYSTEM_PYTHON: "0"
      UV_NO_PROGRESS: "1"
    init: true
    volumes:
      # ─────────────────────────────────────────────────────────────────
      # 1. 全量命名卷（核心）替代所有分散挂载
      #    包含: .global, .local, .opencode, .openclaw, .claude
      #          .notebooklm, .agents, .cache, go/, /app
      # ─────────────────────────────────────────────────────────────────
      - openclaw-devkit-home:/home/node:rw

      # ─────────────────────────────────────────────────────────────────
      # 2. 只读挂载：CLI 工具配置文件（容器内可读，宿主机可编辑）
      #    注意：必须只读，避免宿主机编辑器锁文件或权限问题
      # ─────────────────────────────────────────────────────────────────
      - ${HOME:-${USERPROFILE:-/tmp}}/.claude:/home/node/.claude:ro
      - ${HOME:-${USERPROFILE:-/tmp}}/.openclaw-in-docker:/home/node/.openclaw:ro
      - ${HOME:-${USERPROFILE:-/tmp}}/.notebooklm:/home/node/.notebooklm:ro
      - ${HOME:-${USERPROFILE:-/tmp}}/.agents/skills:/home/node/.agents/skills:ro

      # ─────────────────────────────────────────────────────────────────
      # 3. 读写挂载：初始化脚本和动态工作区
      # ─────────────────────────────────────────────────────────────────
      - ./docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh:ro
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace:rw
    healthcheck: &common-healthcheck
      # Use 127.0.0.1 for more stable local check
      test: [ "CMD", "node", "-e", "require('http').get('http://127.0.0.1:18789/healthz', (res) => process.exit(res.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))" ]
      interval: 10s
      timeout: 10s
      retries: 3
      start_period: 30s
    labels:
      - "openclaw.service=gateway"
      - "openclaw.environment=dev"
